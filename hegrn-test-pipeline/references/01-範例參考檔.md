# 範例參考檔（references 示範）

這個檔案示範「references 子資料夾」也會被一起同步。

同步後，它會以獨立一份文件出現在 📁 `hegrn-test-pipeline` 分組底下，標題顯示為本檔第一個中文 `#` 標題（也就是「範例參考檔（references 示範）」）。

實務上，你可以把 SKILL.md 當「索引/總綱」，把冗長的內容拆進 references/ 的多個檔案，維持每份文件精簡好讀。
